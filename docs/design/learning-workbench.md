# 持续学习工作台（Issue #8）

## 续作方案与验收（2026-09-09）

用户已接受首批实现。本次保留其持续容器，整合远端 `main@7ef99f9` 的 #3/#4/#5/#6，以下方案取代旧记录中的依赖缺口判断。

流程：全局选区 → 区内资料集合/文档/章节 → 导入叶子向导 → 结构化目标草稿与资料范围 → 按目标选择已有教学会话或明确新建 → 作答/反馈/自由问答/恢复焦点 → 复习 → 返回原页。保存目标本身不创建或切换教学会话。Agent 聊天历史保持独立入口。

实施顺序：先接入前置契约并解决合并重叠；再扩展容器的资源导航、多字段表单和嵌入叶子生命周期；整合资料树与范围选择、目标管理、显式教学会话和原导入模型；最后补齐教学呈现及统一验收。UI 不解析命令 stdout，不新增审批或评分规则，目标调用 saveGoal/setGoalField，教学调用指定会话应用动作，导入嵌入原 importModel。

验收：同一全屏完成上述主要链路；创建目标零教学副作用；A/B 区及 A/B 会话草稿与迟到响应隔离；返回恢复选择与滚动；中文多字段、多行粘贴可用；导入预览与确认保留正式规则和未知结果语义；旧服务、清除、失败与合法空结果明确区分；大小屏幕和 resize 可返回；Agent/picker/任务/凭据/非 TTY 回归通过。Linux 原生 PTY、HTTP/应用集成、CLI 全量及定向 race 分别记录；macOS 原生若环境不可用则如实列出，不能用交叉构建代替。

## 本轮交付与验收（2026-09-09）

续作缺口确认：接受版本 `b9a989c` 的 `command/workbench.go:68` 仍明确标注资料树/审批未接入；第 122 行仍依赖旧当前会话入口，第 130–131 行仍通过旧目标与会话联动接口保存目标。这些不是已有设置可打开的能力。远端 `main@7ef99f9` 已提供集合/范围、独立目标、显式会话、完整导入模型，因此本轮只替换外壳适配与展示，不重做前置领域实现。

已完成：

- 资料集合、文档/章节树、全文浏览、集合名称搜索、全局共享目录与区内引用、冻结及追加范围；目标可绑定该范围。
- 目标搜索/分页、结构化多字段编辑、历史修订与独立生命周期；保存目标不触碰教学。新会话绑定用户刚查看的目标修订，教学会话按名称选择，不回退旧 `current` 接口。
- 原 `importModel` 同屏嵌入，继续使用扫描、身份审批、正式预览与原子确认；返回保留本地输入、清单位置，重入重查集合并使旧预览失效。未知提交保留原操作和凭证；异步返回目标页前冻结回执上下文。
- 题目、作答、讲解、反馈、评估确认/覆盖/作废、自由问答与恢复焦点；共享原应用动作及不可变证据校验，没有 stdout 解析、评分或状态机副本。
- 页面身份/会话与题目级草稿、真实返回历史、多字段枚举输入、大小屏 resize、可取消请求和过期响应过滤；更换服务器不复用原展示容器，隐私清除释放展示草稿及叶子缓存。Agent/picker/受管任务仍使用原资源所有权。

验证环境：Linux 6.8.0-117-generic / amd64，Go 1.26.6。证据绑定本轮提交的 `clients/cli-go` 源码、测试和依赖；仅改文档不使对应代码证据失效。

| 层次 | 实际证据与结论 |
| --- | --- |
| 结构化应用集成 | `TestWorkbenchLibraryGoalAndExplicitSessionFlow`：资料→章节→冻结→多行目标→独立教学，目标保存零教学写入，全程零旧 current 读取和命令输出；通过 |
| 教学适配 | `TestWorkbenchAnswerUsesCLIApplicationAction`、旧上下文拒绝测试及 command 原有教学/评估测试；通过 |
| 容器与叶子生命周期 | 多字段草稿/稳定失败重试身份、会话/题目隔离、返回滚动、慢请求/迟到结果、隐私清除、过小尺寸；`TestEmbeddedImportResumePreservesLocalPositionAndRevalidates` 验证重入位置、预览失效、禁止绕过外壳切区、未知操作保留；通过 |
| Linux 原生 PTY | `TestImportWizardPTYScanPreviewConfirm/工作台嵌入`：真实 Bubble Tea/PTY + HTTP 夹具，中文多行粘贴→服务端预览→确认→资料页，返回前未输出退出 alternate screen 序列；通过。原 dashboard PTY 提交/切页及独立导入 PTY 亦通过 |
| CLI 候选检查 | `go test ./...`、`go vet ./...`、`go build ./...`；通过。最终局部修正后重跑 command/workbench 相关测试、vet 与构建，未将缓存复用称为新的独立验收 |
| 并发 | `go test -race ./internal/workbench ./internal/dashboard ./internal/command`；通过，叶子返回上下文修正后又通过 workbench/command 定向 race |
| 差异与边界 | `git diff --check` 通过；没有新服务端/schema/协议改动（相对前置远端），不重跑前置数据库矩阵 |
| 独立待验收 | 当前环境没有 macOS 原生终端；没有把交叉编译或 Linux PTY 冒充 macOS 证据。HTTP 夹具不等于真实 PostgreSQL + 在线模型端到端，本轮未运行该组合 |

本外壳不提供后续导入后台任务、规划、进度统计或 Agent 区域绑定。非默认区跨目标复习总览依现有服务能力明确不可用；不以全局数据或空列表冒充成功。全部正式验收仍须独立审查，尤其 macOS 原生交互。

## 首批确认与方案（以下为已接受的历史记录）

基线为 `68d507d`。`dashboard/dashboard.go:400–407` 的目标和导入提交直接返回 `tea.Quit`；`command/app.go:239–246` 在全屏程序退出后执行命令并等待回车；`dashboard/dashboard.go:51` 每次创建新模型。因而不是缺少某个配置，而是容器生命周期只覆盖菜单。`command/learn.go:74` 的学习循环仍使用同步终端输入。

架构：服务端 knowledge/learning/tutoring owner 保存权威状态，CLI api 校验协议，command 提供应用动作，Bubble Tea 负责交互。新增容器只保存进程内表单、选择与滚动位置；command 适配器返回页面数据，复用已有应用动作，不解析命令输出，不修改服务端状态机。Agent、凭据设置继续使用原入口和资源所有权。

操作流程：主菜单 → 工作台 → 全局学习区选择 → 区内概览/资料/目标/学习/复习 → 返回原页。输入模式下 Enter 换行，Ctrl+S 提交，Esc 返回；浏览模式下通过可见菜单和快捷键导航。

```text
学习工作台  当前区：默认学习区
当前目标：理解图论   教学阶段：AwaitingResponse
概览 | 资料 | 目标 | 学习 | 复习 | 切区 | 帮助 | 返回主菜单
──────────────────────────────────────────
题目 / 反馈 / 当前页列表（可滚动）
操作选择器 / 多行输入框
加载、错误、取消结果与按键提示
```

窄屏使用同一个纵向操作选择器；过小屏幕保留退出与返回。异步请求绑定页面、空间和请求序号，取消后旧结果不可覆盖新页面。每次进入页面重新读取服务端，展示内容不作为缓存权威来源；错误清空旧内容。草稿不写磁盘，按学习区与页面隔离。

## 实施批次和验收

1. 持续容器、可见导航、输入焦点、中文多行、滚动、草稿隔离、异步取消和迟到响应测试。
2. 复用 #2 学习区查询；接入当前目标、教学投影与复习结构化结果及既有学习动作。不存在的资料树、历史目标选择和完整导入向导明确报告依赖尚未接入，不能自行创造第二套业务规则。
3. 更新用户说明；运行受影响包测试与 race、CLI 测试/vet/build，记录 Linux PTY 证据。macOS 原生交互与真实服务完整链路需独立记录，交叉编译不算验收。

完整 Issue 验收依赖 #3–#6 叶子能力；当前分支只有 #2，不能把本次容器及现有能力整合表述为所有依赖已完成。资料隔离、目标历史管理、导入审批向导、并行教学会话均保持各 owner 归属。

## 实施结果与证据（2026-09-08）

生产入口：已配对主菜单 `b`；原 `z/g/i/l/w` 直接进入持续页面。容器集成在同一个 Bubble Tea 程序中。`command/workbench.go` 从公共 API 和现有应用动作构造页面；目标、导入、答案提交提取共享的结构化结果。命令输出不作为页面数据源。只有原设置、传统审批及 Agent 等独立入口继续使用原生命周期。

实际完成：全局学习区搜索/分页/选择；区内概览、资料检索、简单 Markdown 导入、目标创建/切换；诊断/路线、出题、作答和帮助等级、评估、确认评估、自由问答与恢复焦点；到期复习。动作受服务端允许动作、会话版本和归档状态约束。取消/迟到结果不覆盖当前页；冲突明确失败，不静默重放。草稿按学习区/页保存，学习输入另绑定会话/目标/活动。服务端返回隐私清除错误时释放草稿。

| 验收方面 | 本次结论 |
| --- | --- |
| 同一全屏内提交和切页 | 通过 Linux PTY：中文多行粘贴、提交、切换复习，提交前后没有离开 alternate screen |
| 结构化应用动作复用 | 通过 HTTP 集成：服务端题目投影、中文答案、帮助等级、expected_version 与 CLI 契约一致，无命令输出或本地草稿持久化 |
| 返回、滚动、跨区隔离 | 通过容器测试：目标草稿、多行输入、滚动恢复、A/B 区隔离 |
| 异步、取消、迟到结果 | 通过受控慢服务测试与 race；切页触发取消，旧结果被丢弃 |
| 错误、确认、安全渲染、尺寸 | 通过失败后刷新、取消确认、隐私清除释放、控制字符和过小终端返回测试；PTY 发现主菜单溢出后已修正 |
| 非默认区与过期上下文 | 通过 HTTP 集成：不进入默认业务；旧会话不产生写请求 |
| Agent、凭据和降级回归 | CLI 全量测试通过，包含原有 Agent/picker/本地任务、凭据及非 TTY 检查；没有新增 Agent 资源清理路径 |
| 完整资料树/目标选择/导入审批向导 | 尚未完成，依赖 #3/#4/#6；UI 显示限制。原身份审批入口仍可使用 |
| 讲解呈现、评估覆盖/作废 | 工作台尚未接入；现有显式 learn 能力保留，界面帮助已注明 |
| macOS 原生与真实服务/模型完整链路 | 未运行；Linux PTY 与 fake HTTP 不替代这些验收 |

实际通过的命令（在 `clients/cli-go`）：`go test ./...`、`go vet ./...`、`go build ./...`，以及 `go test -race ./internal/workbench ./internal/dashboard ./internal/command`。`git diff --check` 通过。未修改服务端、数据库或协议，本次不运行数据库矩阵；不把旧数据库测试证据归入本次。

本次可审查持续外壳与现有业务整合，但不能据此关闭 #8 的全部验收清单。资料树、历史目标、完整审批以及上述原生/真实服务证据仍需后续集成。
