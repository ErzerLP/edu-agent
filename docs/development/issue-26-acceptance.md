# Issue #26 实施与验收记录

## 确认与实施

基线 `f9b455e`。先核对 learning、tutoring、knowledge、learningcontent、mentorrun、
隐私屏障和 Web/CLI 协议，确认导师没有正式变更工具，既有规划拒绝作答期间改路线。
具体证据、开发方案与验收标准见 [教学变更设计](../design/adaptive-learning-changes.md)。

新增不可变候选修订、差异、基础版本、审批摘要与交互绑定、持久队列、原子应用及补偿版本。
导师实际调用服务；解释直接追加，同目标路线按模式接入，目标标准必须具体确认。
立即切换保留原题、答案、评分合同和焦点；撤回不删除学习事实。
同步 API、能力协商、CLI 查询、Web 时间线与模式、隐私清理和部署代理。

## 已执行验证

- 服务端 `go test ./...` 通过；无数据库环境时的跳过不算数据库验收。
- 受影响服务端包 `go vet`、服务端 `go build ./...` 通过。
- CLI API/命令包测试、`go vet`、`go build ./...` 通过。
- 现有本机 PostgreSQL 测试容器内使用独立随机 schema，未接触生产数据。
  五个 `TestPostgreSQLAdaptive*` 场景分别通过：空资料开学后的真实会话变更、排队与立即切换、
  补偿、审批修订与摘要、跨区/版本拒绝、正文直接追加、故障回滚、并发冲突、
  真实模型工具调用、已接收答案保留、目标修订、暂停/归档/撤销/全局清除。
- `TestPostgreSQLLearningChangeHTTPContract` 通过，覆盖真实 HTTP/OpenAPI 与协议边界。
- 新增浏览器差异、草稿与焦点用例及前端协议单测，但尚未运行。

## 明确未完成的验证

`clients/web` 尚无依赖，实际运行 `npm run check` 返回 `tsc: not found`。
已询问安装授权但未获得，因此没有安装或升级。Web 类型检查、标准类型生成、Vitest、
Vite/发行资产构建与 Playwright 仍待执行；手写的新协议路径类型不能代替标准生成验收。
没有向真实外部模型或搜索供应商发起请求，也没有执行真实部署验收。

用户随后接受任务并要求落地主分支；合并时仍须保留上述限制，不把接受等同于未运行检查通过。

## 合并主分支时的手工冲突处理

主分支 `1f29ce5` 已包含 #25。手工合并了七处文件：Web 导师 DTO、浏览器工作区测试、
OpenAPI、导师 runtime fixture、导师存储、导师类型及迁移测试。
两套模型 fixture、运行类型、API 与浏览器场景均保留；自动合并的正文提交逻辑仍保留 #25 的来源链与变更记录。
保留已发布的 `000025_content_collaboration.sql`，将本项迁移顺延为 `000026_learning_changes.sql`。

合并后重新执行服务端与 CLI `go test ./...` 均通过；服务端及 CLI 构建、受影响服务端包 vet 通过。
真实 PG 下使用 `-race -p=1` 串行验证五个 Adaptive 场景和三个 #25 Content 场景（76.827 秒），
变更 HTTP 合同（3.168 秒）；另行验证旧内容版本/答案/清除（6.670 秒）。迁移顺序与唯一性检查通过。
这些结果不包含前述未运行的 Web 检查。
